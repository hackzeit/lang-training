#include <vector> // Para Static()

typedef unsigned long long uint64;
//using uint64 = unsigned long long;

namespace factorial
{
  uint64 Direc(uint64);
  uint64 Recur(uint64);
  uint64 Static(uint64);
}
